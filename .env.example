# Postgres connection string (free options: Neon.tech, Supabase, Railway)
DATABASE_URL=postgres://user:password@host:5432/dbname?sslmode=require

# Gmail SMTP for sending result emails (use a Gmail App Password, not your normal password)
EMAIL_USER=youraddress@gmail.com
EMAIL_PASS=your16charapppassword

# Where the admin copy of every submission is sent
ADMIN_EMAIL=posh.82@gmail.com
